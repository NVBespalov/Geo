'use strict';

var React = require('react/addons');

require('styles/GeoDetails.less');

var GeoDetails = React.createClass({
    render: function () {
        return (
            <div>
                <p>Content for GeoDetails</p>
            </div>
        );
    }
});

module.exports = GeoDetails;

